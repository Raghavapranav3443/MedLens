feat: README claims-to-proof rewrite, compare endpoint, glossary, PDF upload, dead-dep removal

- README: PS requirements matrix (requirement -> implementation -> proof),
  safety architecture, mermaid diagram, security model, Cloud Run deploy,
  honest limitations + anti-features
- GET /api/records/[id]/compare: deterministic longitudinal comparison
- Record page: changes-vs-previous section, plain-language glossary popovers
- sources: text-layer PDF upload (multipart, 5-page/5MiB/40k-char caps, scans
  get a paste-the-text path)
- Removed unused @google/genai dependency; removed stale repo artifacts
- 93/93 tests, production build green (12 routes)
