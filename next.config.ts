import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Standalone output: required for the Cloud Run container (smaller image,
  // faster cold start on the free tier's scale-to-zero).
  output: "standalone",
  poweredByHeader: false,
  reactStrictMode: true,

  // Security headers the PRD promises — configured here so they are grep-able
  // and apply to every route. Cloud Run terminates TLS, so these are safe.
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "X-Frame-Options", value: "DENY" },
          { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
          {
            key: "Permissions-Policy",
            value: "camera=(), microphone=(), geolocation=()",
          },
          {
            key: "Content-Security-Policy",
            value:
              "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'",
          },
          {
            key: "Strict-Transport-Security",
            value: "max-age=63072000; includeSubDomains; preload",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
